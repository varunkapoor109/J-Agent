<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Opportunities - JobAgent</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/output.css" rel="stylesheet">
</head>
<body class="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-100">

  <!-- Subtle background -->
  <div class="fixed inset-0 overflow-hidden pointer-events-none">
    <div class="absolute -top-40 -right-40 w-80 h-80 bg-primary-200 rounded-full mix-blend-multiply filter blur-3xl opacity-20"></div>
    <div class="absolute -bottom-40 -left-40 w-80 h-80 bg-warm-200 rounded-full mix-blend-multiply filter blur-3xl opacity-20"></div>
  </div>

  <div class="relative min-h-screen">
    <!-- Header -->
    <header class="bg-white/80 backdrop-blur-lg border-b border-gray-200 sticky top-0 z-50">
      <div class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
        <a href="/" class="flex items-center space-x-3">
          <div class="w-10 h-10 bg-gradient-to-br from-primary-500 to-indigo-600 rounded-xl flex items-center justify-center shadow-lg">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
            </svg>
          </div>
          <span class="text-xl font-semibold text-gray-800">JobAgent</span>
        </a>

        <a href="/upload" class="btn-secondary text-sm py-2 px-4">
          <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"></path>
          </svg>
          Upload New Resume
        </a>
      </div>
    </header>

    <!-- Main content -->
    <main class="max-w-6xl mx-auto px-4 py-8">

      <!-- Summary section -->
      <div class="bg-white/80 backdrop-blur-lg rounded-2xl shadow-lg p-6 mb-8 border border-white/50 animate-fade-in">
        <div class="flex flex-col md:flex-row md:items-center md:justify-between">
          <div>
            <h1 class="text-2xl md:text-3xl font-semibold text-gray-800 mb-2">
              Great news! We found opportunities for you
            </h1>
            <p class="text-gray-600">
              Based on your experience as a <span class="font-medium text-primary-600"><%= resumeData.primaryRole?.title || 'Professional' %></span>
              with <span class="font-medium"><%= resumeData.totalYearsExperience || 'several' %> years</span> of experience
            </p>
          </div>
          <div class="mt-4 md:mt-0 flex items-center space-x-4">
            <div class="text-center px-4 py-2 bg-green-50 rounded-lg">
              <p class="text-2xl font-bold text-green-600"><%= recommended.length %></p>
              <p class="text-xs text-green-700">Recommended</p>
            </div>
            <div class="text-center px-4 py-2 bg-blue-50 rounded-lg">
              <p class="text-2xl font-bold text-blue-600"><%= worthExploring.length %></p>
              <p class="text-xs text-blue-700">Worth Exploring</p>
            </div>
          </div>
        </div>

        <!-- Skills matched -->
        <% if (resumeData.skills?.technical?.length > 0) { %>
        <div class="mt-4 pt-4 border-t border-gray-100">
          <p class="text-sm text-gray-500 mb-2">Your top skills:</p>
          <div class="flex flex-wrap gap-2">
            <% resumeData.skills.technical.slice(0, 8).forEach(skill => { %>
              <span class="px-3 py-1 bg-primary-50 text-primary-700 rounded-full text-sm font-medium"><%= skill %></span>
            <% }) %>
          </div>
        </div>
        <% } %>
      </div>

      <!-- Encouraging message for career pivoters -->
      <% if (resumeData.totalYearsExperience > 5 && (resumeData.experienceByRole?.[resumeData.primaryRole?.type]?.years || 0) < 3) { %>
      <div class="bg-warm-50 border border-warm-200 rounded-xl p-4 mb-8 animate-fade-in" style="animation-delay: 0.1s;">
        <div class="flex items-start">
          <div class="flex-shrink-0">
            <svg class="w-5 h-5 text-warm-500 mt-0.5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z"></path>
            </svg>
          </div>
          <p class="ml-3 text-sm text-gray-700">
            <span class="font-medium">Career transitions take courage!</span> We've focused on roles that match your current experience level in this field. Your transferable skills from previous roles will make you a strong candidate.
          </p>
        </div>
      </div>
      <% } %>

      <!-- Steps indicator -->
      <div class="flex items-center justify-center space-x-4 text-sm mb-8 animate-fade-in" style="animation-delay: 0.15s;">
        <div class="flex items-center text-gray-400">
          <div class="w-8 h-8 rounded-full bg-green-100 text-green-600 flex items-center justify-center font-medium">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
          </div>
          <span class="ml-2">Upload</span>
        </div>
        <div class="w-8 h-px bg-green-300"></div>
        <div class="flex items-center text-gray-400">
          <div class="w-8 h-8 rounded-full bg-green-100 text-green-600 flex items-center justify-center font-medium">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
          </div>
          <span class="ml-2">Analyze</span>
        </div>
        <div class="w-8 h-px bg-green-300"></div>
        <div class="flex items-center text-primary-600">
          <div class="w-8 h-8 rounded-full bg-primary-600 text-white flex items-center justify-center font-medium">3</div>
          <span class="ml-2 font-medium">Results</span>
        </div>
      </div>

      <!-- Recommended Jobs Section -->
      <% if (recommended.length > 0) { %>
      <section class="mb-10 animate-slide-up" style="animation-delay: 0.2s;">
        <div class="flex items-center mb-6">
          <div class="w-10 h-10 bg-green-100 rounded-xl flex items-center justify-center mr-3">
            <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <div>
            <h2 class="text-xl font-semibold text-gray-800">Recommended</h2>
            <p class="text-sm text-gray-500">Best matches with 90-95% confidence</p>
          </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          <% recommended.forEach(job => { %>
            <%- include('partials/jobCard', { job, type: 'recommended' }) %>
          <% }) %>
        </div>
      </section>
      <% } %>

      <!-- Worth Exploring Section -->
      <% if (worthExploring.length > 0) { %>
      <section class="mb-10 animate-slide-up" style="animation-delay: 0.3s;">
        <div class="flex items-center mb-6">
          <div class="w-10 h-10 bg-blue-100 rounded-xl flex items-center justify-center mr-3">
            <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
            </svg>
          </div>
          <div>
            <h2 class="text-xl font-semibold text-gray-800">Worth Exploring</h2>
            <p class="text-sm text-gray-500">Good matches with 70-89% confidence</p>
          </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          <% worthExploring.forEach(job => { %>
            <%- include('partials/jobCard', { job, type: 'exploring' }) %>
          <% }) %>
        </div>
      </section>
      <% } %>

      <!-- No results -->
      <% if (recommended.length === 0 && worthExploring.length === 0) { %>
      <div class="text-center py-12">
        <div class="w-16 h-16 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
        </div>
        <h3 class="text-lg font-medium text-gray-800 mb-2">No matches found yet</h3>
        <p class="text-gray-600 mb-4">We couldn't find strong matches right now. Try uploading an updated resume with more details.</p>
        <a href="/upload" class="btn-primary">Try Again</a>
      </div>
      <% } %>

      <!-- Encouragement footer -->
      <div class="text-center py-8 border-t border-gray-200 mt-8 animate-fade-in" style="animation-delay: 0.4s;">
        <p class="text-gray-500 text-sm mb-2">Remember:</p>
        <p class="text-gray-700 italic">"The only way to do great work is to love what you do. Keep searching!"</p>
        <p class="text-gray-400 text-xs mt-1">- Steve Jobs</p>
      </div>

    </main>
  </div>

</body>
</html>
